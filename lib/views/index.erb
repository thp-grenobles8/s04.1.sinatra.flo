<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>Mon super site de gossip, depuis le dossier views !</h1>
    <a href="/gossips/new/">Nouveau potin</a>
    <% gossips.each_with_index do |gossip, index| %>
        <p>
          <a href="/gossips/<%= index %>/"> Potin n°<%= index%></a></br>
          <%= gossip.author %><br/>
          <%= gossip.content %>
        </p>
      <% end %>
  </body>
</html>
